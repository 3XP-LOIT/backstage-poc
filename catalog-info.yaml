apiVersion backstage.io/v1alpha1
kind: Component
metadata:
  name: exploit-variant-2
  annotations:
    # This tells Backstage where to find the mkdocs.yml
    backstage.io/techdocs-ref: dir:. 
spec:
  type: service
  lifecycle: experimental
  owner: guests
